export default function getVariables() {
    const slug = 'qwefixSnakeGameReactRss';
    if (localStorage.getItem(slug) == undefined) {
        console.log('und')
    }

    // return {
    //     CANVAS_SIZE,
    //     SNAKE_START,
    //     APPLE_START,
    //     SCALE,
    //     SPEED,
    //     DIRECTIONS,
    // }
}